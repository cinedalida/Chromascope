/**
 * ProductSwatchPicker — FUTURE USE (ARTryOnPage)
 *
 * Horizontally scrollable row of color swatches for selecting AR product shades.
 * Works with AppliedProductsPanel to preview and apply product colors on the AR feed.
 * Receives a products array and an onSelect callback.
 */
export function ProductSwatchPicker() {
  return (
    <div className="flex gap-3 overflow-x-auto">
      <button className="h-12 w-12 rounded-full bg-purple-500" />
    </div>
  );
}
