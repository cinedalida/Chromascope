/**
 * AROverlayCanvas — FUTURE USE (ARTryOnPage)
 *
 * Canvas layer rendered on top of CameraFeed to composite makeup product overlays.
 * Integrates with WebGL / TensorFlow Face Mesh for real-time landmark tracking.
 * Receives product color/opacity data from AppliedProductsPanel state.
 */
export function AROverlayCanvas() {
  return (
    <div className="rounded-3xl bg-slate-900 p-6 text-center text-slate-300">AR overlay canvas placeholder</div>
  );
}
