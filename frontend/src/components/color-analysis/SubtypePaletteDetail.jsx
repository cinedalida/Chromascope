/**
 * SubtypePaletteDetail — FUTURE USE (ColorAnalysisSubtypePage)
 *
 * Detail view for a specific seasonal subtype: palette swatches, biometric metrics,
 * and sister/contrast season cards. Extracts the inline content from ColorAnalysisSubtypePage.
 * Receives `subtype`, `palette`, and `metrics` props.
 */
export function SubtypePaletteDetail() {
  return (
    <div className="rounded-3xl bg-slate-900 p-5 text-slate-300">
      <p>Subtype palette detail explains the color families suited for your undertone.</p>
    </div>
  );
}
