/**
 * ProductCatalogTable — FUTURE USE (AdminDatabasePage)
 *
 * Paginated admin table for browsing and managing the product catalog.
 * Will replace the inline table currently hardcoded in ProductCatalogPage.
 * Receives data from adminService / productStore.
 */
export function ProductCatalogTable() {
  return (
    <div className="overflow-x-auto rounded-3xl bg-slate-900 p-4 text-slate-300">Admin product catalog placeholder</div>
  );
}
