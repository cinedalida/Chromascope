/**
 * AddProductModal — FUTURE USE (AdminDatabasePage)
 *
 * A dialog/modal for adding new products to the catalog.
 * Wire to AdminDatabasePage when the admin CRUD flow is implemented.
 * Will use: adminService.saveProduct(), productStore, and the shared Modal component.
 */
export function AddProductModal() {
  return (
    <div className="rounded-3xl bg-slate-900 p-5 text-slate-300">Add product modal placeholder</div>
  );
}
